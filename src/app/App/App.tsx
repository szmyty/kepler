/**
 * App Component
 * - Uses the Router component to define the application's routing structure.
 */
import Router from "../Router";

const App: React.FC = () => {
  return <Router />;
};

export default App;
